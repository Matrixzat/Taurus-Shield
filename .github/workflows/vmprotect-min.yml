name: VM Protect (nmmp)
on:
  workflow_dispatch:
    inputs:
      asset_id:
        required: true
      job_id:
        required: true
      sign_apk:
        required: false
        default: 'true'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ok asset=${{ inputs.asset_id }}"
